_format_version: "2.1"

services:
  - host: asrdisponibilidad_upstream
    name: asrdisponibilidad_service
    protocol: http
    routes:
      
      - name: asrdisponibilidad
        paths:
          - /
        strip_path: false

  - host: subirdocumento_upstream
    name: subirdocumento_service
    protocol: http
    routes:
      
      - name: subirdocumento
        paths:
          - /subirdocumento
          - /subirdocumentoValidate/
        strip_path: false
    
upstreams:
  - name: subirdocumento_upstream
    targets:
      - target: <ip-privada-subirdocumento-app-a>:8080
        weight: 100
      - target: <ip-privada-subirdocumento-app-b>:8080
        weight: 100
      - target: <ip-privada-subirdocumento-app-c>:8080
        weight: 100
    #healthchecks:
    #  threshold: 0
    #  active:
    #    http_path: /health/
    #    timeout: 0
    #    healthy:
    #      successes: 0
    #      interval: 0
    #    unhealthy:
    #      tcp_failures: 0
    #      interval: 0

  - name: asrdisponibilidad_upstream
    targets:
      - target: <ip-privada-asrdisponibilidad-app>:8080
        weight: 100


